using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using Acme.Collections;

namespace learningCSharp
{
    public class MainTest
    {
        public static void Main(string[] args)
        {
            // LambdaTest.test();
            // LambdaTest.testScope(5);
            // Console.WriteLine(LambdaTest.del2(10));
            // AnonymousMethodTest.test();

            // Stack s = new Stack();
            // s.Push(1);
            // s.Push(10);
            // s.Push(100);
            // Console.WriteLine(s.Pop());
            // Console.WriteLine(s.Pop());
            // Console.WriteLine(s.Pop());

            // StatementTest.CheckedUnchecked();

            // StatementTest.YieldStatement(null);

            // foreach(var number in StatementTest.GetNumbers())
            //     Console.WriteLine(number);

            // foreach(var number in StatementTest.GenerateWithoutYield())
            //     Console.WriteLine(number);
            // foreach(var number in StatementTest.GenerateWithYield())
            //     Console.WriteLine(number);

            // ClassTest.Pair pair = new ClassTest.Pair{ First = 1, Second = "2"};

            // ClassTest.SwapExample();
            // ClassTest.OutUsage();
            ClassTest.WriteSquares();
        }
    }
}